# AMC Backend — Python dependencies
# Install with:  pip install -r requirements.txt
#
# Pinned to ranges rather than exact versions so they stay installable
# across Python 3.10–3.12 environments.

fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
python-multipart>=0.0.9,<1.0.0
pydantic>=2.7.0,<3.0.0
tensorflow>=2.16.1
numpy>=1.26.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0
